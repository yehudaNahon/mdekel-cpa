---
export const prerender = false;
import { isLocale } from "../../i18n/config";
const locale = isLocale(Astro.params.lang) ? Astro.params.lang : "he";
return Astro.redirect(`/${locale}/`, 302);
---
